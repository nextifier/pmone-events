diff --git a/apps/flei/app/components/Orb.vue b/apps/flei/app/components/Orb.vue
index e30fc40..d4075ac 100644
--- a/apps/flei/app/components/Orb.vue
+++ b/apps/flei/app/components/Orb.vue
@@ -251,12 +251,8 @@ const setupAnimation = () => {
 
   let targetHover = 0;
   let lastTime = 0;
-  let lastRender = 0;
   let currentRot = 0;
   const rotationSpeed = 0.3;
-  // Cap the noise shader at ~30fps. It's an ambient background, so half the
-  // frames are imperceptible but cut the sustained GPU/main-thread cost in half.
-  const frameInterval = 1000 / 30;
 
   const handleMouseMove = (e: MouseEvent) => {
     const rect = container.getBoundingClientRect();
@@ -297,10 +293,6 @@ const setupAnimation = () => {
       return;
     }
 
-    // Throttle to ~30fps.
-    if (t - lastRender < frameInterval) return;
-    lastRender = t;
-
     const dt = (t - lastTime) * 0.001;
     lastTime = t;
     program.uniforms.iTime.value = t * 0.001;
