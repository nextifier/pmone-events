diff --git a/apps/flei/app/components/HeroVisual26th.vue b/apps/flei/app/components/HeroVisual26th.vue
index f7a791a..294a275 100644
--- a/apps/flei/app/components/HeroVisual26th.vue
+++ b/apps/flei/app/components/HeroVisual26th.vue
@@ -37,11 +37,19 @@
         class="size-full translate-y-[5%]"
       >
         <template #item="{ item, index }">
-          <img
+          <!-- Cloudflare provider transforms to webp on the custom domain.
+               First frame is the LCP candidate: eager + high priority + preload. -->
+          <NuxtImg
             :src="item.image"
             :alt="item.text || `Hero gallery ${index + 1}`"
+            width="800"
+            height="1000"
+            sizes="320px md:400px"
+            format="webp"
             class="block size-full object-cover"
-            loading="lazy"
+            :loading="index === 0 ? 'eager' : 'lazy'"
+            :fetchpriority="index === 0 ? 'high' : 'low'"
+            :preload="index === 0"
           />
         </template>
       </Carousel3d>
diff --git a/apps/flei/app/components/Orb.vue b/apps/flei/app/components/Orb.vue
index 5e2d9ff..8bf945f 100644
--- a/apps/flei/app/components/Orb.vue
+++ b/apps/flei/app/components/Orb.vue
@@ -1,5 +1,5 @@
 <script setup lang="ts">
-import { onMounted, onUnmounted, watch, useTemplateRef } from "vue";
+import { onMounted, onUnmounted, ref, useTemplateRef } from "vue";
 import { Renderer, Program, Mesh, Triangle, Vec3 } from "ogl";
 
 interface OrbProps {
@@ -18,6 +18,20 @@ const props = withDefaults(defineProps<OrbProps>(), {
 
 const ctnDom = useTemplateRef<HTMLDivElement>("ctnDom");
 
+// Render-gating: only burn GPU/main-thread on the noise shader while the orb is
+// actually on-screen and the tab is visible. Honour reduced-motion too.
+const isVisible = ref(true);
+const documentVisibility = useDocumentVisibility();
+const reducedMotion = useMediaQuery("(prefers-reduced-motion: reduce)");
+
+useIntersectionObserver(
+  ctnDom,
+  ([entry]) => {
+    isVisible.value = entry?.isIntersecting ?? true;
+  },
+  { threshold: 0 },
+);
+
 const vert = /* glsl */ `
     precision highp float;
     attribute vec2 position;
@@ -212,7 +226,9 @@ const setupAnimation = () => {
 
   function resize() {
     if (!container) return;
-    const dpr = window.devicePixelRatio || 1;
+    // Cap DPR: this is a fill-rate-bound noise shader, so rendering at full 3x/4x
+    // mobile DPR costs ~9-16x the fragment work for negligible visual gain.
+    const dpr = Math.min(window.devicePixelRatio || 1, 2);
     const width = container.clientWidth;
     const height = container.clientHeight;
     renderer.setSize(width * dpr, height * dpr);
@@ -261,6 +277,16 @@ const setupAnimation = () => {
   let rafId: number;
   const update = (t: number) => {
     rafId = requestAnimationFrame(update);
+
+    // Skip the expensive WebGL render when off-screen or the tab is hidden.
+    // RAF keeps ticking (negligible cost) so rendering resumes instantly.
+    const offscreen =
+      !isVisible.value || documentVisibility.value === "hidden";
+    if (offscreen) {
+      lastTime = t; // avoid a dt spike on resume
+      return;
+    }
+
     const dt = (t - lastTime) * 0.001;
     lastTime = t;
     program.uniforms.iTime.value = t * 0.001;
@@ -277,6 +303,11 @@ const setupAnimation = () => {
     program.uniforms.rot.value = currentRot;
 
     renderer.render({ scene: mesh });
+
+    // Reduced-motion: paint one static frame, then stop animating.
+    if (reducedMotion.value) {
+      cancelAnimationFrame(rafId);
+    }
   };
   rafId = requestAnimationFrame(update);
 
@@ -301,17 +332,10 @@ onUnmounted(() => {
   }
 });
 
-watch(
-  () => props,
-  () => {
-    if (cleanupAnimation) {
-      cleanupAnimation();
-      cleanupAnimation = null;
-    }
-    setupAnimation();
-  },
-  { deep: true },
-);
+// NOTE: the previous `watch(() => props, ..., { deep: true })` was removed. It
+// tore down and rebuilt the entire GL context/geometry/listeners on every prop
+// access, causing TBT spikes + flicker. All props (hue, hoverIntensity,
+// rotateOnHover, forceHoverState) are already read live inside the RAF loop.
 </script>
 
 <template>
diff --git a/layers/base/app/components/BrandPreview.vue b/layers/base/app/components/BrandPreview.vue
index 549552f..142ee8f 100644
--- a/layers/base/app/components/BrandPreview.vue
+++ b/layers/base/app/components/BrandPreview.vue
@@ -41,7 +41,6 @@
             v-for="brand in layout.brands"
             :key="brand.slug"
             :to="localePath(`/brands/${brand.slug}`)"
-            :aria-label="brand.brand_name"
             class="group hover:bg-muted/40 flex flex-col items-center overflow-hidden px-2 pt-6 pb-6 transition-colors"
           >
             <Avatar
@@ -107,7 +106,9 @@ const content = computed(() => contentStore.components.brandPreview);
 
 // Fetch the full active set (not just a page) so the score sort below picks the
 // globally highest-scoring brands, not merely the highest within the first page.
-const { data, pending } = await useAsyncData("brand-preview", () =>
+// Lazy + non-blocking: the skeleton (GridFill pending) holds the layout while
+// data streams in, so this never blocks first paint.
+const { data, pending } = useLazyAsyncData("brand-preview", () =>
   $fetch("/api/exhibitors", { query: { per_page: 200 } }).catch(() => null),
 );
 
diff --git a/layers/base/app/components/Credits.vue b/layers/base/app/components/Credits.vue
index 7cceeeb..e5d0888 100644
--- a/layers/base/app/components/Credits.vue
+++ b/layers/base/app/components/Credits.vue
@@ -35,7 +35,7 @@
               'h-20 xl:h-24': partner.list.length <= 10,
               'h-18 xl:h-20': partner.list.length > 10,
             }"
-            aria-label="Partner logo"
+            :aria-label="item.link ? item.name || 'Partner logo' : undefined"
           >
             <NuxtImg
               :src="`${partner.directory}${item.img}`"
diff --git a/layers/base/app/components/Partnerships.vue b/layers/base/app/components/Partnerships.vue
index 381b1b7..ec71484 100644
--- a/layers/base/app/components/Partnerships.vue
+++ b/layers/base/app/components/Partnerships.vue
@@ -65,6 +65,7 @@
           <nuxt-link
             :to="lp(item.link)"
             :target="item.link.startsWith('http') ? '_blank' : ''"
+            :aria-label="item.title"
             class="bg-muted aspect-square w-full shrink-0 overflow-hidden rounded-xl lg:size-32"
           >
             <NuxtImg
diff --git a/layers/base/app/composables/useCurrentTime.js b/layers/base/app/composables/useCurrentTime.js
index 349bc43..90b920e 100644
--- a/layers/base/app/composables/useCurrentTime.js
+++ b/layers/base/app/composables/useCurrentTime.js
@@ -1,14 +1,22 @@
 const now = ref(new Date());
 
-let interval;
+let timer;
 let activeInstances = 0;
 
+function scheduleTick() {
+  // Align each tick to the next whole-second boundary instead of a fixed 1000ms
+  // interval. Same once-per-second update, but no drift and no wasted wakeups.
+  const delay = 1000 - (Date.now() % 1000);
+  timer = setTimeout(() => {
+    now.value = new Date();
+    scheduleTick();
+  }, delay);
+}
+
 export function useCurrentTime() {
   onMounted(() => {
     if (activeInstances === 0) {
-      interval = setInterval(() => {
-        now.value = new Date();
-      }, 1000);
+      scheduleTick();
     }
     activeInstances++;
   });
@@ -16,7 +24,7 @@ export function useCurrentTime() {
   onUnmounted(() => {
     activeInstances--;
     if (activeInstances === 0) {
-      clearInterval(interval);
+      clearTimeout(timer);
     }
   });
 
diff --git a/layers/base/app/plugins/vue-tippy.ts b/layers/base/app/plugins/vue-tippy.ts
index 6418917..01ded74 100644
--- a/layers/base/app/plugins/vue-tippy.ts
+++ b/layers/base/app/plugins/vue-tippy.ts
@@ -1,10 +1,9 @@
 import VueTippy from "vue-tippy";
 import type { TippyPluginOptions } from "vue-tippy";
 
-import "tippy.js/animations/scale.css";
+// Only "shift-away" is actually used (see defaultProps.animation below). The
+// other three animation stylesheets were render-blocking dead weight.
 import "tippy.js/animations/shift-away.css";
-import "tippy.js/animations/shift-toward.css";
-import "tippy.js/animations/perspective.css";
 
 export default defineNuxtPlugin((nuxtApp) => {
   nuxtApp.vueApp.use(VueTippy, {
diff --git a/layers/base/nuxt.config.ts b/layers/base/nuxt.config.ts
index 8d26177..3f88ba5 100644
--- a/layers/base/nuxt.config.ts
+++ b/layers/base/nuxt.config.ts
@@ -45,6 +45,15 @@ export default defineNuxtConfig({
           sizes: "180x180",
           href: "/icons/apple-touch-icon.png",
         },
+        // Preload the variable font used for the hero headline (LCP text). The
+        // crossorigin attr is required so this matches the CORS font fetch.
+        {
+          rel: "preload",
+          as: "font",
+          type: "font/woff2",
+          href: "/fonts/MinusOne-VF.woff2",
+          crossorigin: "",
+        },
       ],
       script: [],
     },
@@ -98,8 +107,16 @@ export default defineNuxtConfig({
     "nuxt-gtag",
     "@formkit/auto-animate/nuxt",
     "@vite-pwa/nuxt",
+    "nuxt-vitalizer",
   ],
 
+  vitalizer: {
+    // Strip the <link rel="prefetch"> tags Nuxt emits for every dynamic-import
+    // chunk. They download JS the current page never runs (PageSpeed: "Reduce
+    // unused JavaScript"). Navigation still works — chunks load on demand.
+    disablePrefetchLinks: true,
+  },
+
   fonts: {
     families: [
       {
diff --git a/layers/base/package.json b/layers/base/package.json
index 51910fb..7d607bc 100644
--- a/layers/base/package.json
+++ b/layers/base/package.json
@@ -37,6 +37,7 @@
     "gsap": "^3.14.2",
     "lucide-vue-next": "^0.577.0",
     "nuxt-gtag": "^4.1.0",
+    "nuxt-vitalizer": "^2.0.0",
     "pinia": "^3.0.4",
     "qrcode": "^1.5.4",
     "reka-ui": "^2.9.2",
diff --git a/pnpm-lock.yaml b/pnpm-lock.yaml
index b1491e0..5fb760b 100644
--- a/pnpm-lock.yaml
+++ b/pnpm-lock.yaml
@@ -285,6 +285,9 @@ importers:
       nuxt-gtag:
         specifier: ^4.1.0
         version: 4.1.0(magicast@0.5.2)
+      nuxt-vitalizer:
+        specifier: ^2.0.0
+        version: 2.0.0(magicast@0.5.2)
       pinia:
         specifier: ^3.0.4
         version: 3.0.4(typescript@5.9.3)(vue@3.5.30(typescript@5.9.3))
@@ -5160,6 +5163,9 @@ packages:
   nuxt-site-config@3.2.21:
     resolution: {integrity: sha512-WCqo4cirBc+GLPBZOU1ye5+f4xjC7Sf7qbKt/zpeCtEUqJLHDR0MoKICfsGt/8EdkSDYUo+m5BNZ1oxai0isgQ==}
 
+  nuxt-vitalizer@2.0.0:
+    resolution: {integrity: sha512-X2mabvcXMfKmWc46p8WmCbTlr8+wDniPHunQIRSGWmH0goqw6YTAe0+ttxRNISGxcobXDqqip7qPCE6+T60t1g==}
+
   nuxt@4.4.2:
     resolution: {integrity: sha512-iWVFpr/YEqVU/CenqIHMnIkvb2HE/9f+q8oxZ+pj2et+60NljGRClCgnmbvGPdmNFE0F1bEhoBCYfqbDOCim3Q==}
     engines: {node: ^20.19.0 || >=22.12.0}
@@ -12034,6 +12040,12 @@ snapshots:
       - vite
       - vue
 
+  nuxt-vitalizer@2.0.0(magicast@0.5.2):
+    dependencies:
+      '@nuxt/kit': 4.4.2(magicast@0.5.2)
+    transitivePeerDependencies:
+      - magicast
+
   nuxt@4.4.2(@babel/core@7.29.0)(@babel/plugin-syntax-jsx@7.28.6(@babel/core@7.29.0))(@parcel/watcher@2.5.6)(@types/node@25.5.0)(@vue/compiler-sfc@3.5.30)(cac@6.7.14)(commander@13.1.0)(db0@0.3.4)(eslint@10.0.2(jiti@2.6.1))(ioredis@5.10.1)(lightningcss@1.32.0)(magicast@0.5.2)(optionator@0.9.4)(pinia@3.0.4(typescript@5.9.3)(vue@3.5.30(typescript@5.9.3)))(rollup-plugin-visualizer@7.0.1(rollup@4.60.0))(rollup@4.60.0)(terser@5.46.1)(typescript@5.9.3)(vite@7.3.1(@types/node@25.5.0)(jiti@2.6.1)(lightningcss@1.32.0)(terser@5.46.1)(yaml@2.8.3))(yaml@2.8.3):
     dependencies:
       '@dxup/nuxt': 0.4.0(magicast@0.5.2)(typescript@5.9.3)
