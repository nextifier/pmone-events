diff --git a/dist/Drawer/DrawerContentImpl.cjs b/dist/Drawer/DrawerContentImpl.cjs
index 67acbe8a35ad60cd74ba4e92a025d06b44920d6e..af5254b31d58d45493b370c4e5bc3d063268065e 100644
--- a/dist/Drawer/DrawerContentImpl.cjs
+++ b/dist/Drawer/DrawerContentImpl.cjs
@@ -54,7 +54,7 @@ var DrawerContentImpl_vue_vue_type_script_setup_true_lang_default = /* @__PURE__
 		const rootContext = require_Drawer_DrawerRoot.injectDrawerRootContext();
 		const { forwardRef, currentElement } = require_shared_useForwardExpose.useForwardExpose();
 		require_Drawer_utils.registerDrawerCssProperties();
-		const { activeSnapPointOffset, snapToNearest } = require_composables_useDrawerSnapPoints.useDrawerSnapPoints({
+		const { activeSnapPointOffset, resolvedSnapPoints, snapToNearest } = require_composables_useDrawerSnapPoints.useDrawerSnapPoints({
 			snapPoints: rootContext.snapPoints,
 			activeSnapPoint: rootContext.activeSnapPoint,
 			popupHeight: rootContext.popupHeight,
@@ -64,6 +64,32 @@ var DrawerContentImpl_vue_vue_type_script_setup_true_lang_default = /* @__PURE__
 				else rootContext.setActiveSnapPoint(point);
 			}
 		});
+		const snapPointRange = (0, vue.computed)(() => {
+			const pts = resolvedSnapPoints.value;
+			const dir = rootContext.swipeDirection.value;
+			if (!pts || pts.length < 2 || dir !== "down" && dir !== "up") return null;
+			const offsets = pts.map((point) => point.offset).sort((a, b) => a - b);
+			const range = offsets[1] - offsets[0];
+			if (!(range > 0)) return null;
+			return {
+				minOffset: offsets[0],
+				range
+			};
+		});
+		const offsetToSnapProgress = (offset) => {
+			const r = snapPointRange.value;
+			if (!r) return null;
+			return Math.min(1, Math.max(0, (offset - r.minOffset) / r.range));
+		};
+		const snapPointProgress = (0, vue.computed)(() => {
+			const offset = activeSnapPointOffset.value;
+			if (offset == null) return null;
+			return offsetToSnapProgress(offset);
+		});
+(0, vue.watch)([snapPointProgress, () => rootContext.open.value], ([p, isOpen]) => {
+			if (p == null) return;
+			rootContext.onNestedSwipeProgressChange(isOpen ? p : 0);
+		}, { immediate: true });
 		function writeSnapPointOffset() {
 			const el = currentElement.value;
 			if (!el) return;
@@ -78,9 +104,21 @@ var DrawerContentImpl_vue_vue_type_script_setup_true_lang_default = /* @__PURE__
 		(0, __vueuse_core.useResizeObserver)(currentElement, ([entry]) => {
 			if (!entry) return;
 			if (rootContext.hasNestedDrawer.value && rootContext.popupHeight.value > 0) return;
-			const h = entry.contentRect.height;
+			const h = entry.borderBoxSize?.[0]?.blockSize ?? entry.target.offsetHeight;
 			rootContext.onPopupHeightChange(h);
-			currentElement.value?.style.setProperty(require_Drawer_utils.DRAWER_CSS_VARS.height, `${h}px`);
+		});
+		(0, vue.watch)([
+			() => rootContext.hasNestedDrawer.value,
+			() => rootContext.open.value,
+			() => rootContext.popupHeight.value
+		], ([nested, isOpen, measured]) => {
+			const heightEl = currentElement.value;
+			if (!heightEl) return;
+			if (!nested && isOpen) heightEl.style.removeProperty(require_Drawer_utils.DRAWER_CSS_VARS.height);
+			else if (measured > 0) heightEl.style.setProperty(require_Drawer_utils.DRAWER_CSS_VARS.height, `${measured}px`);
+		}, {
+			immediate: true,
+			flush: "post"
 		});
 		(0, vue.watch)(() => rootContext.frontmostHeight.value, (h) => {
 			currentElement.value?.style.setProperty(require_Drawer_utils.DRAWER_CSS_VARS.frontmostHeight, `${h}px`);
@@ -105,6 +143,7 @@ var DrawerContentImpl_vue_vue_type_script_setup_true_lang_default = /* @__PURE__
 			enabled: (0, vue.computed)(() => rootContext.open.value),
 			elementRef: currentElement,
 			directions: swipeDirections,
+			swipeThreshold: ({ element, direction }) => Math.max((direction === "left" || direction === "right" ? element.offsetWidth : element.offsetHeight) * .5, 10),
 			movementCssVars: {
 				x: require_Drawer_utils.DRAWER_CSS_VARS.swipeMovementX,
 				y: require_Drawer_utils.DRAWER_CSS_VARS.swipeMovementY
@@ -151,7 +190,23 @@ var DrawerContentImpl_vue_vue_type_script_setup_true_lang_default = /* @__PURE__
 					x: details.deltaX,
 					y: details.deltaY
 				};
-				rootContext.onNestedSwipeProgressChange(progress);
+				if (hasSnapPoints.value && rootContext.swipeDirection.value === "down") {
+					const snapEl = currentElement.value;
+					if (snapEl && details) {
+						const base = activeSnapPointOffset.value ?? 0;
+						const next = base + details.deltaY;
+						const movement = next >= 0 ? details.deltaY : -Math.sqrt(-next) - base;
+						snapEl.style.setProperty(require_Drawer_utils.DRAWER_CSS_VARS.swipeMovementY, `${movement}px`);
+					}
+				}
+				let resolvedProgress = progress;
+				const range = snapPointRange.value;
+				if (range && rootContext.popupHeight.value > 0 && details && Number.isFinite(details.deltaY)) {
+					const base = activeSnapPointOffset.value ?? range.minOffset;
+					const nextOffset = Math.min(rootContext.popupHeight.value, Math.max(0, base + details.deltaY));
+					resolvedProgress = offsetToSnapProgress(nextOffset) ?? progress;
+				}
+				rootContext.onNestedSwipeProgressChange(resolvedProgress);
 			}
 		});
 		let pendingDismissReason;
@@ -217,6 +272,8 @@ var DrawerContentImpl_vue_vue_type_script_setup_true_lang_default = /* @__PURE__
 			};
 			if (isSwiping.value) attrs["data-swiping"] = "";
 			if (rootContext.hasNestedDrawer.value) attrs["data-nested-drawer-open"] = "";
+			if (rootContext.nestedSwiping.value) attrs["data-nested-drawer-swiping"] = "";
+			if (rootContext.activeSnapPoint.value === 1) attrs["data-expanded"] = "";
 			return attrs;
 		});
 		(0, vue.watch)(() => rootContext.nestedOpenDrawerCount.value, (n) => {
diff --git a/dist/Drawer/DrawerContentImpl.js b/dist/Drawer/DrawerContentImpl.js
index 99b7e735737335445ced7746ad8f118da912e8bc..d3d1955f97e39db9dcc12c5e2363d7e4e0c00826 100644
--- a/dist/Drawer/DrawerContentImpl.js
+++ b/dist/Drawer/DrawerContentImpl.js
@@ -53,7 +53,7 @@ var DrawerContentImpl_vue_vue_type_script_setup_true_lang_default = /* @__PURE__
 		const rootContext = injectDrawerRootContext();
 		const { forwardRef, currentElement } = useForwardExpose();
 		registerDrawerCssProperties();
-		const { activeSnapPointOffset, snapToNearest } = useDrawerSnapPoints({
+		const { activeSnapPointOffset, resolvedSnapPoints, snapToNearest } = useDrawerSnapPoints({
 			snapPoints: rootContext.snapPoints,
 			activeSnapPoint: rootContext.activeSnapPoint,
 			popupHeight: rootContext.popupHeight,
@@ -63,6 +63,32 @@ var DrawerContentImpl_vue_vue_type_script_setup_true_lang_default = /* @__PURE__
 				else rootContext.setActiveSnapPoint(point);
 			}
 		});
+		const snapPointRange = computed(() => {
+			const pts = resolvedSnapPoints.value;
+			const dir = rootContext.swipeDirection.value;
+			if (!pts || pts.length < 2 || dir !== "down" && dir !== "up") return null;
+			const offsets = pts.map((point) => point.offset).sort((a, b) => a - b);
+			const range = offsets[1] - offsets[0];
+			if (!(range > 0)) return null;
+			return {
+				minOffset: offsets[0],
+				range
+			};
+		});
+		const offsetToSnapProgress = (offset) => {
+			const r = snapPointRange.value;
+			if (!r) return null;
+			return Math.min(1, Math.max(0, (offset - r.minOffset) / r.range));
+		};
+		const snapPointProgress = computed(() => {
+			const offset = activeSnapPointOffset.value;
+			if (offset == null) return null;
+			return offsetToSnapProgress(offset);
+		});
+watch([snapPointProgress, () => rootContext.open.value], ([p, isOpen]) => {
+			if (p == null) return;
+			rootContext.onNestedSwipeProgressChange(isOpen ? p : 0);
+		}, { immediate: true });
 		function writeSnapPointOffset() {
 			const el = currentElement.value;
 			if (!el) return;
@@ -77,9 +103,21 @@ var DrawerContentImpl_vue_vue_type_script_setup_true_lang_default = /* @__PURE__
 		useResizeObserver(currentElement, ([entry]) => {
 			if (!entry) return;
 			if (rootContext.hasNestedDrawer.value && rootContext.popupHeight.value > 0) return;
-			const h$1 = entry.contentRect.height;
+			const h$1 = entry.borderBoxSize?.[0]?.blockSize ?? entry.target.offsetHeight;
 			rootContext.onPopupHeightChange(h$1);
-			currentElement.value?.style.setProperty(DRAWER_CSS_VARS.height, `${h$1}px`);
+		});
+		watch([
+			() => rootContext.hasNestedDrawer.value,
+			() => rootContext.open.value,
+			() => rootContext.popupHeight.value
+		], ([nested, isOpen, measured]) => {
+			const el$1 = currentElement.value;
+			if (!el$1) return;
+			if (!nested && isOpen) el$1.style.removeProperty(DRAWER_CSS_VARS.height);
+			else if (measured > 0) el$1.style.setProperty(DRAWER_CSS_VARS.height, `${measured}px`);
+		}, {
+			immediate: true,
+			flush: "post"
 		});
 		watch(() => rootContext.frontmostHeight.value, (h$1) => {
 			currentElement.value?.style.setProperty(DRAWER_CSS_VARS.frontmostHeight, `${h$1}px`);
@@ -104,6 +142,7 @@ var DrawerContentImpl_vue_vue_type_script_setup_true_lang_default = /* @__PURE__
 			enabled: computed(() => rootContext.open.value),
 			elementRef: currentElement,
 			directions: swipeDirections,
+			swipeThreshold: ({ element, direction }) => Math.max((direction === "left" || direction === "right" ? element.offsetWidth : element.offsetHeight) * .5, 10),
 			movementCssVars: {
 				x: DRAWER_CSS_VARS.swipeMovementX,
 				y: DRAWER_CSS_VARS.swipeMovementY
@@ -150,7 +189,23 @@ var DrawerContentImpl_vue_vue_type_script_setup_true_lang_default = /* @__PURE__
 					x: details.deltaX,
 					y: details.deltaY
 				};
-				rootContext.onNestedSwipeProgressChange(progress);
+				if (hasSnapPoints.value && rootContext.swipeDirection.value === "down") {
+					const el$1 = currentElement.value;
+					if (el$1 && details) {
+						const base = activeSnapPointOffset.value ?? 0;
+						const next = base + details.deltaY;
+						const movement = next >= 0 ? details.deltaY : -Math.sqrt(-next) - base;
+						el$1.style.setProperty(DRAWER_CSS_VARS.swipeMovementY, `${movement}px`);
+					}
+				}
+				let resolvedProgress = progress;
+				const range = snapPointRange.value;
+				if (range && rootContext.popupHeight.value > 0 && details && Number.isFinite(details.deltaY)) {
+					const base = activeSnapPointOffset.value ?? range.minOffset;
+					const nextOffset = Math.min(rootContext.popupHeight.value, Math.max(0, base + details.deltaY));
+					resolvedProgress = offsetToSnapProgress(nextOffset) ?? progress;
+				}
+				rootContext.onNestedSwipeProgressChange(resolvedProgress);
 			}
 		});
 		let pendingDismissReason;
@@ -216,6 +271,8 @@ var DrawerContentImpl_vue_vue_type_script_setup_true_lang_default = /* @__PURE__
 			};
 			if (isSwiping.value) attrs["data-swiping"] = "";
 			if (rootContext.hasNestedDrawer.value) attrs["data-nested-drawer-open"] = "";
+			if (rootContext.nestedSwiping.value) attrs["data-nested-drawer-swiping"] = "";
+			if (rootContext.activeSnapPoint.value === 1) attrs["data-expanded"] = "";
 			return attrs;
 		});
 		watch(() => rootContext.nestedOpenDrawerCount.value, (n) => {
diff --git a/dist/composables/useDrawerSnapPoints.cjs b/dist/composables/useDrawerSnapPoints.cjs
index a09ba2ade9ab36a22bd2a9376dcae9ed0b692dc0..632669032ba0ad75feff17bf000a4678aa1b21cd 100644
--- a/dist/composables/useDrawerSnapPoints.cjs
+++ b/dist/composables/useDrawerSnapPoints.cjs
@@ -7,6 +7,7 @@ const SNAP_VELOCITY_THRESHOLD = .5;
 const SNAP_VELOCITY_MULTIPLIER = 300;
 const MAX_SNAP_VELOCITY = 4;
 const FAST_SWIPE_VELOCITY = .5;
+const MIN_SWIPE_THRESHOLD = 10;
 function parseSnapPoint(value, viewportHeight, rootFontSize) {
 	let result = null;
 	if (typeof value === "number") result = value >= 0 && value <= 1 ? value * viewportHeight : value;
@@ -67,47 +68,64 @@ function useDrawerSnapPoints(options) {
 		if (points.length === 0) return;
 		const ph = popupHeight.value;
 		const axisVel = direction === "up" || direction === "down" ? velocity.y : velocity.x;
-		const velSigned = direction === "up" || direction === "left" ? -axisVel : axisVel;
+		let velSigned = direction === "up" || direction === "left" ? -axisVel : axisVel;
+		const dragDirection = Math.sign(dragDelta);
+		if (dragDirection !== 0 && Math.abs(dragDelta) >= MIN_SWIPE_THRESHOLD) {
+			const reversal = Math.sign(velSigned);
+			if (reversal !== 0 && reversal !== dragDirection) velSigned = 0;
+		}
 		const activePoint = findActiveResolvedPoint();
 		const currentOffset = activePoint?.offset ?? 0;
 		const dragTargetOffset = Math.max(0, Math.min(ph, currentOffset + dragDelta));
-		let targetOffset = dragTargetOffset;
-		if (Math.abs(velSigned) >= SNAP_VELOCITY_THRESHOLD) {
-			const clampedVel = Math.max(-MAX_SNAP_VELOCITY, Math.min(MAX_SNAP_VELOCITY, velSigned));
-			targetOffset = dragTargetOffset + clampedVel * SNAP_VELOCITY_MULTIPLIER;
-		}
-		let closest = points[0];
-		let closestDist = Math.abs(targetOffset - closest.offset);
-		for (const p of points) {
-			const d = Math.abs(targetOffset - p.offset);
-			if (d < closestDist) {
-				closest = p;
-				closestDist = d;
+		const velocityOffset = Math.abs(velSigned) >= SNAP_VELOCITY_THRESHOLD ? Math.max(-MAX_SNAP_VELOCITY, Math.min(MAX_SNAP_VELOCITY, velSigned)) * SNAP_VELOCITY_MULTIPLIER : 0;
+		const targetOffset = sequential ? dragTargetOffset : Math.max(0, Math.min(ph, dragTargetOffset + velocityOffset));
+		const sorted = [...points].sort((a, b) => a.offset - b.offset);
+		const nearestIndex = (offset) => {
+			let index = 0;
+			let best = Infinity;
+			for (let i = 0; i < sorted.length; i += 1) {
+				const d = Math.abs(sorted[i].offset - offset);
+				if (d < best) {
+					best = d;
+					index = i;
+				}
 			}
-		}
-		const closeDistance = Math.abs(targetOffset - ph);
-		if (closeDistance < closestDist) {
-			onSnapPointChange(null);
-			return;
-		}
+			return index;
+		};
 		if (sequential) {
-			const sorted = [...points].sort((a, b) => a.offset - b.offset);
-			const currentIdx = activePoint ? sorted.indexOf(activePoint) : -1;
-			if (currentIdx < 0) {
-				onSnapPointChange(closest.value);
-				return;
-			}
-			const dragDir = Math.sign(dragDelta);
-			const velDir = Math.sign(velSigned);
-			const shouldAdvance = velDir === dragDir && Math.abs(velSigned) >= FAST_SWIPE_VELOCITY;
-			const adjacentIdx = Math.max(0, Math.min(sorted.length - 1, currentIdx + dragDir));
-			const adjacent = sorted[adjacentIdx];
+			const currentIndex = nearestIndex(currentOffset);
+			let targetSnapPoint = sorted[nearestIndex(targetOffset)];
+			let effectiveTargetOffset = targetOffset;
+			const velDirection = Math.sign(velSigned);
+			const shouldAdvance = dragDirection !== 0 && velDirection !== 0 && velDirection === dragDirection && Math.abs(velSigned) >= SNAP_VELOCITY_THRESHOLD;
 			if (shouldAdvance) {
-				onSnapPointChange(adjacent.value);
+				const adjacentIndex = Math.max(0, Math.min(sorted.length - 1, currentIndex + dragDirection));
+				if (adjacentIndex !== currentIndex) {
+					const adjacentPoint = sorted[adjacentIndex];
+					const shouldForceAdjacent = dragDirection > 0 ? targetOffset < adjacentPoint.offset : targetOffset > adjacentPoint.offset;
+					if (shouldForceAdjacent) {
+						targetSnapPoint = adjacentPoint;
+						effectiveTargetOffset = adjacentPoint.offset;
+					}
+				} else if (dragDirection > 0) {
+					onSnapPointChange(null);
+					return;
+				}
+			}
+			if (Math.abs(effectiveTargetOffset - ph) < Math.abs(effectiveTargetOffset - targetSnapPoint.offset)) {
+				onSnapPointChange(null);
 				return;
 			}
-			const crossed = dragDir > 0 ? targetOffset > adjacent.offset : targetOffset < adjacent.offset;
-			onSnapPointChange(crossed ? adjacent.value : activeSnapPoint.value ?? closest.value);
+			onSnapPointChange(targetSnapPoint.value);
+			return;
+		}
+		if (velSigned >= FAST_SWIPE_VELOCITY && dragDelta > 0) {
+			onSnapPointChange(null);
+			return;
+		}
+		const closest = sorted[nearestIndex(targetOffset)];
+		if (Math.abs(targetOffset - ph) < Math.abs(targetOffset - closest.offset)) {
+			onSnapPointChange(null);
 			return;
 		}
 		onSnapPointChange(closest.value);
diff --git a/dist/composables/useDrawerSnapPoints.js b/dist/composables/useDrawerSnapPoints.js
index 4dd1803e5df3ab8b85d25888547b99002cc95585..d447208e48d0d8af588c1640e605710440e3ce05 100644
--- a/dist/composables/useDrawerSnapPoints.js
+++ b/dist/composables/useDrawerSnapPoints.js
@@ -6,6 +6,7 @@ const SNAP_VELOCITY_THRESHOLD = .5;
 const SNAP_VELOCITY_MULTIPLIER = 300;
 const MAX_SNAP_VELOCITY = 4;
 const FAST_SWIPE_VELOCITY = .5;
+const MIN_SWIPE_THRESHOLD = 10;
 function parseSnapPoint(value, viewportHeight, rootFontSize) {
 	let result = null;
 	if (typeof value === "number") result = value >= 0 && value <= 1 ? value * viewportHeight : value;
@@ -66,47 +67,64 @@ function useDrawerSnapPoints(options) {
 		if (points.length === 0) return;
 		const ph = popupHeight.value;
 		const axisVel = direction === "up" || direction === "down" ? velocity.y : velocity.x;
-		const velSigned = direction === "up" || direction === "left" ? -axisVel : axisVel;
+		let velSigned = direction === "up" || direction === "left" ? -axisVel : axisVel;
+		const dragDirection = Math.sign(dragDelta);
+		if (dragDirection !== 0 && Math.abs(dragDelta) >= MIN_SWIPE_THRESHOLD) {
+			const reversal = Math.sign(velSigned);
+			if (reversal !== 0 && reversal !== dragDirection) velSigned = 0;
+		}
 		const activePoint = findActiveResolvedPoint();
 		const currentOffset = activePoint?.offset ?? 0;
 		const dragTargetOffset = Math.max(0, Math.min(ph, currentOffset + dragDelta));
-		let targetOffset = dragTargetOffset;
-		if (Math.abs(velSigned) >= SNAP_VELOCITY_THRESHOLD) {
-			const clampedVel = Math.max(-MAX_SNAP_VELOCITY, Math.min(MAX_SNAP_VELOCITY, velSigned));
-			targetOffset = dragTargetOffset + clampedVel * SNAP_VELOCITY_MULTIPLIER;
-		}
-		let closest = points[0];
-		let closestDist = Math.abs(targetOffset - closest.offset);
-		for (const p of points) {
-			const d = Math.abs(targetOffset - p.offset);
-			if (d < closestDist) {
-				closest = p;
-				closestDist = d;
+		const velocityOffset = Math.abs(velSigned) >= SNAP_VELOCITY_THRESHOLD ? Math.max(-MAX_SNAP_VELOCITY, Math.min(MAX_SNAP_VELOCITY, velSigned)) * SNAP_VELOCITY_MULTIPLIER : 0;
+		const targetOffset = sequential ? dragTargetOffset : Math.max(0, Math.min(ph, dragTargetOffset + velocityOffset));
+		const sorted = [...points].sort((a, b) => a.offset - b.offset);
+		const nearestIndex = (offset) => {
+			let index = 0;
+			let best = Infinity;
+			for (let i = 0; i < sorted.length; i += 1) {
+				const d = Math.abs(sorted[i].offset - offset);
+				if (d < best) {
+					best = d;
+					index = i;
+				}
 			}
-		}
-		const closeDistance = Math.abs(targetOffset - ph);
-		if (closeDistance < closestDist) {
-			onSnapPointChange(null);
-			return;
-		}
+			return index;
+		};
 		if (sequential) {
-			const sorted = [...points].sort((a, b) => a.offset - b.offset);
-			const currentIdx = activePoint ? sorted.indexOf(activePoint) : -1;
-			if (currentIdx < 0) {
-				onSnapPointChange(closest.value);
-				return;
-			}
-			const dragDir = Math.sign(dragDelta);
-			const velDir = Math.sign(velSigned);
-			const shouldAdvance = velDir === dragDir && Math.abs(velSigned) >= FAST_SWIPE_VELOCITY;
-			const adjacentIdx = Math.max(0, Math.min(sorted.length - 1, currentIdx + dragDir));
-			const adjacent = sorted[adjacentIdx];
+			const currentIndex = nearestIndex(currentOffset);
+			let targetSnapPoint = sorted[nearestIndex(targetOffset)];
+			let effectiveTargetOffset = targetOffset;
+			const velDirection = Math.sign(velSigned);
+			const shouldAdvance = dragDirection !== 0 && velDirection !== 0 && velDirection === dragDirection && Math.abs(velSigned) >= SNAP_VELOCITY_THRESHOLD;
 			if (shouldAdvance) {
-				onSnapPointChange(adjacent.value);
+				const adjacentIndex = Math.max(0, Math.min(sorted.length - 1, currentIndex + dragDirection));
+				if (adjacentIndex !== currentIndex) {
+					const adjacentPoint = sorted[adjacentIndex];
+					const shouldForceAdjacent = dragDirection > 0 ? targetOffset < adjacentPoint.offset : targetOffset > adjacentPoint.offset;
+					if (shouldForceAdjacent) {
+						targetSnapPoint = adjacentPoint;
+						effectiveTargetOffset = adjacentPoint.offset;
+					}
+				} else if (dragDirection > 0) {
+					onSnapPointChange(null);
+					return;
+				}
+			}
+			if (Math.abs(effectiveTargetOffset - ph) < Math.abs(effectiveTargetOffset - targetSnapPoint.offset)) {
+				onSnapPointChange(null);
 				return;
 			}
-			const crossed = dragDir > 0 ? targetOffset > adjacent.offset : targetOffset < adjacent.offset;
-			onSnapPointChange(crossed ? adjacent.value : activeSnapPoint.value ?? closest.value);
+			onSnapPointChange(targetSnapPoint.value);
+			return;
+		}
+		if (velSigned >= FAST_SWIPE_VELOCITY && dragDelta > 0) {
+			onSnapPointChange(null);
+			return;
+		}
+		const closest = sorted[nearestIndex(targetOffset)];
+		if (Math.abs(targetOffset - ph) < Math.abs(targetOffset - closest.offset)) {
+			onSnapPointChange(null);
 			return;
 		}
 		onSnapPointChange(closest.value);
diff --git a/dist/composables/useSwipeDismiss.cjs b/dist/composables/useSwipeDismiss.cjs
index d3fc83619a981fd880205d7b135f4aea49ca5f34..b18f630e6d0655c8aee81e1024806bfa559ccfc4 100644
--- a/dist/composables/useSwipeDismiss.cjs
+++ b/dist/composables/useSwipeDismiss.cjs
@@ -10,6 +10,7 @@ const MIN_DRAG_THRESHOLD = 1;
 const MIN_RELEASE_VELOCITY_DURATION_MS = 16;
 const MAX_RELEASE_VELOCITY_AGE_MS = 80;
 const DEFAULT_IGNORE_SELECTOR = "button,a,input,select,textarea,label,[role=\"button\"]";
+const SWIPE_IGNORE_SELECTOR = "[data-drawer-swipe-ignore],[data-base-ui-swipe-ignore],[data-swipe-ignore],input[type=\"range\"],[role=\"slider\"]";
 function findScrollableAncestor(el, axis) {
 	if (!el || el === document.body) return null;
 	const style = window.getComputedStyle(el);
@@ -128,6 +129,11 @@ function useSwipeDismiss(options) {
 		el.style.setProperty(movementCssVars.y, "0px");
 	}
 	function reset() {
+		const rubberBandEl = elementRef.value;
+		if (rubberBandEl?.hasAttribute("data-rubber-band")) {
+			rubberBandEl.removeAttribute("data-rubber-band");
+			clearCssVars(rubberBandEl);
+		}
 		setSwiping(false);
 		swipeDirection.value = void 0;
 		dragOffset.value = {
@@ -205,7 +211,16 @@ function useSwipeDismiss(options) {
 		const dy = lockedAxis === "horizontal" ? 0 : rawDy;
 		const dir = (0, vue.toValue)(directions).find((d) => require_Drawer_utils.getDisplacement(d, dx, dy) > 0);
 		if (pendingSwipe && pendingSwipeStartPos) {
-			if (!dir) return;
+			if (!dir) {
+				const idle = applyDirectionalDamping(dx, dy);
+				dragOffset.value = {
+					x: idle.x,
+					y: idle.y
+				};
+				setCssVars(el, idle.x, idle.y);
+				if (idle.x !== 0 || idle.y !== 0) el.setAttribute("data-rubber-band", "");
+				return;
+			}
 			const pending = require_Drawer_utils.getDisplacement(dir, pos.x - pendingSwipeStartPos.x, pos.y - pendingSwipeStartPos.y);
 			if (Math.abs(pending) < MIN_DRAG_THRESHOLD) return;
 			pendingSwipe = false;
@@ -234,8 +249,7 @@ function useSwipeDismiss(options) {
 		const currentEl = elementRef.value;
 		if (currentEl) {
 			const dim = currentDir === "up" || currentDir === "down" ? elementSize.height || currentEl.offsetHeight : elementSize.width || currentEl.offsetWidth;
-			const threshold = getThreshold(currentEl, currentDir);
-			const p = Math.min(1, Math.max(0, displacement / (dim + threshold)));
+			const p = Math.min(1, Math.max(0, displacement / dim));
 			if (p !== swipeProgress) {
 				swipeProgress = p;
 				onProgress?.(p, {
@@ -262,9 +276,10 @@ function useSwipeDismiss(options) {
 		const releaseHandled = onRelease?.(velocity) === true;
 		if (!releaseHandled) {
 			const velInDirection = require_Drawer_utils.getDisplacement(intendedDirection ?? (0, vue.toValue)(directions)[0], velocity.x, velocity.y);
-			const shouldDismiss = !cancelledSwipe && (displacement >= threshold || velInDirection > .3);
+			const shouldDismiss = !cancelledSwipe && (displacement >= threshold || velInDirection > .5);
 			if (shouldDismiss) {
 				el.setAttribute("data-swipe-dismissed", "");
+				el.setAttribute("data-swipe-dismiss", "");
 				onDismiss?.();
 			} else {
 				clearCssVars(el);
@@ -279,7 +294,7 @@ function useSwipeDismiss(options) {
 		if (e.button !== 0) return;
 		if (canStart && !canStart()) return;
 		const target = e.target;
-		if (target?.closest(DEFAULT_IGNORE_SELECTOR)) return;
+		if (target?.closest(DEFAULT_IGNORE_SELECTOR) || target?.closest(SWIPE_IGNORE_SELECTOR)) return;
 		const el = elementRef.value;
 		if (!el) return;
 		startSwipe(el, {
@@ -312,7 +327,7 @@ function useSwipeDismiss(options) {
 		if (!(0, vue.toValue)(options.enabled)) return;
 		if (canStart && !canStart()) return;
 		const target = e.target;
-		if (target?.closest(DEFAULT_IGNORE_SELECTOR)) return;
+		if (target?.closest(SWIPE_IGNORE_SELECTOR)) return;
 		const el = elementRef.value;
 		if (!el) return;
 		if (!options.ignoreScrollableAncestors) {
diff --git a/dist/composables/useSwipeDismiss.js b/dist/composables/useSwipeDismiss.js
index 2cb684a5c7bad185c05cb83bd4b4238c8a125488..a11180ad6ca5e70d3fc1bab92aa30c87c5269a1e 100644
--- a/dist/composables/useSwipeDismiss.js
+++ b/dist/composables/useSwipeDismiss.js
@@ -9,6 +9,7 @@ const MIN_DRAG_THRESHOLD = 1;
 const MIN_RELEASE_VELOCITY_DURATION_MS = 16;
 const MAX_RELEASE_VELOCITY_AGE_MS = 80;
 const DEFAULT_IGNORE_SELECTOR = "button,a,input,select,textarea,label,[role=\"button\"]";
+const SWIPE_IGNORE_SELECTOR = "[data-drawer-swipe-ignore],[data-base-ui-swipe-ignore],[data-swipe-ignore],input[type=\"range\"],[role=\"slider\"]";
 function findScrollableAncestor(el, axis) {
 	if (!el || el === document.body) return null;
 	const style = window.getComputedStyle(el);
@@ -127,6 +128,11 @@ function useSwipeDismiss(options) {
 		el.style.setProperty(movementCssVars.y, "0px");
 	}
 	function reset() {
+		const rubberBandEl = elementRef.value;
+		if (rubberBandEl?.hasAttribute("data-rubber-band")) {
+			rubberBandEl.removeAttribute("data-rubber-band");
+			clearCssVars(rubberBandEl);
+		}
 		setSwiping(false);
 		swipeDirection.value = void 0;
 		dragOffset.value = {
@@ -204,7 +210,16 @@ function useSwipeDismiss(options) {
 		const dy = lockedAxis === "horizontal" ? 0 : rawDy;
 		const dir = toValue(directions).find((d) => getDisplacement(d, dx, dy) > 0);
 		if (pendingSwipe && pendingSwipeStartPos) {
-			if (!dir) return;
+			if (!dir) {
+				const idle = applyDirectionalDamping(dx, dy);
+				dragOffset.value = {
+					x: idle.x,
+					y: idle.y
+				};
+				setCssVars(el, idle.x, idle.y);
+				if (idle.x !== 0 || idle.y !== 0) el.setAttribute("data-rubber-band", "");
+				return;
+			}
 			const pending = getDisplacement(dir, pos.x - pendingSwipeStartPos.x, pos.y - pendingSwipeStartPos.y);
 			if (Math.abs(pending) < MIN_DRAG_THRESHOLD) return;
 			pendingSwipe = false;
@@ -233,8 +248,7 @@ function useSwipeDismiss(options) {
 		const currentEl = elementRef.value;
 		if (currentEl) {
 			const dim = currentDir === "up" || currentDir === "down" ? elementSize.height || currentEl.offsetHeight : elementSize.width || currentEl.offsetWidth;
-			const threshold = getThreshold(currentEl, currentDir);
-			const p = Math.min(1, Math.max(0, displacement / (dim + threshold)));
+			const p = Math.min(1, Math.max(0, displacement / dim));
 			if (p !== swipeProgress) {
 				swipeProgress = p;
 				onProgress?.(p, {
@@ -261,9 +275,10 @@ function useSwipeDismiss(options) {
 		const releaseHandled = onRelease?.(velocity) === true;
 		if (!releaseHandled) {
 			const velInDirection = getDisplacement(intendedDirection ?? toValue(directions)[0], velocity.x, velocity.y);
-			const shouldDismiss = !cancelledSwipe && (displacement >= threshold || velInDirection > .3);
+			const shouldDismiss = !cancelledSwipe && (displacement >= threshold || velInDirection > .5);
 			if (shouldDismiss) {
 				el.setAttribute("data-swipe-dismissed", "");
+				el.setAttribute("data-swipe-dismiss", "");
 				onDismiss?.();
 			} else {
 				clearCssVars(el);
@@ -278,7 +293,7 @@ function useSwipeDismiss(options) {
 		if (e.button !== 0) return;
 		if (canStart && !canStart()) return;
 		const target = e.target;
-		if (target?.closest(DEFAULT_IGNORE_SELECTOR)) return;
+		if (target?.closest(DEFAULT_IGNORE_SELECTOR) || target?.closest(SWIPE_IGNORE_SELECTOR)) return;
 		const el = elementRef.value;
 		if (!el) return;
 		startSwipe(el, {
@@ -311,7 +326,7 @@ function useSwipeDismiss(options) {
 		if (!toValue(options.enabled)) return;
 		if (canStart && !canStart()) return;
 		const target = e.target;
-		if (target?.closest(DEFAULT_IGNORE_SELECTOR)) return;
+		if (target?.closest(SWIPE_IGNORE_SELECTOR)) return;
 		const el = elementRef.value;
 		if (!el) return;
 		if (!options.ignoreScrollableAncestors) {
